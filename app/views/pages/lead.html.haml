%form.form-inline
  .form-group
    %label{:for => "name"} Name
    %input#exampleInputName2.form-control{:placeholder => "Jane Doe", :type => "text"}
  .form-group
    %label{:for => "exampleInputEmail2"} Phone
    %input#exampleInputPhone2.form-control{:placeholder => "555-555-1234", :type => "phone"}
  %button.btn.btn-default{:type => "submit"} Call me

:javascript
  $( document ).ready(function() { $( 'form' ).submit(function( event ) { var dataArray = $( this ).serializeArray(), dataObj = {}; $(dataArray).each(function(i, field){ dataObj[field.name] = field.value; }); $.ajax({ method: 'POST', url: 'http://localhost:3000/leads', data: {lead: { fields: dataObj, url: window.location.href, landing_page_id: 1 } } }); event.preventDefault(); }); });